# VITE_API_URL — set this for production builds to point to your API server
# In development, Vite's proxy handles this automatically; do not set it.
# VITE_API_URL=https://api.finbrain.example.com
VITE_CLERK_PUBLISHABLE_KEY=
VITE_DEV_MODE=true
