node_modules/
dist/
.env
.env.local
*.log
.DS_Store
coverage/
.next/
.cache/
*.tsbuildinfo
.turbo/

# Agent context (local only, never push)
AGENTS.md
memory/

# Python
__pycache__/
*.pyc
.venv/
venv/
ml-venv/
*.egg-info/

# ML model files (too large for git)
apps/ml-service/data/xlmr_model/model.safetensors

# Prisma
# Migrations are intentionally tracked: deploys run `prisma migrate deploy`,
# which needs the history in the repository. Ignoring it forces `db push`,
# which mutates databases with no record of what changed.

# Docker
docker-data/

# IDE
.vscode/

# AI agent instructions (local only - never push to dev or main)
# AGENTS.md
.idea/
*.swp
*.swo
apps/web/.vite/
apps/api/uploads/

# Playwright
playwright-report/
test-results/
blob-report/
.playwright/

# Playwright MCP session artifacts
.playwright-mcp/
